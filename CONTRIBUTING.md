# Contributing

Use Node.js 20 or newer and `npm ci`. Run `npm run quality`, `npm audit`, and
`npm pack --dry-run` before opening a pull request. Commits follow Conventional
Commits; public API and security behavior changes must update CHANGELOG and the
migration guide.
